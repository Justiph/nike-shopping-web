
  <section class="py-8 antialiased bg-gray-900 md:py-16">
    <div class="mx-auto max-w-2xl px-4 2xl:px-0">
        <h2 class="text-xl font-semibold text-white sm:text-2xl mb-2">Thanks for your order!</h2>
        <p class="text-gray-400 mb-6 md:mb-8">Your order <a href="#" class="font-medium text-white hover:underline">#7564804</a> will be processed within 24 hours during working days. We will notify you by email once your order has been shipped.</p>
        <div class="space-y-4 sm:space-y-2 rounded-lg border p-6 border-gray-700 bg-gray-800 mb-6 md:mb-8">
            <dl class="sm:flex items-center justify-between gap-4">
                <dt class="font-normal mb-1 sm:mb-0 text-gray-400">Date</dt>
                <dd class="font-medium text-white sm:text-end"><%= orderData.date %></dd>
            </dl>
            <dl class="sm:flex items-center justify-between gap-4">
                <dt class="font-normal mb-1 sm:mb-0 text-gray-400">Payment Method</dt>
                <dd class="font-medium text-white sm:text-end"><%= orderData.paymentMethod %></dd>
            </dl>
            <dl class="sm:flex items-center justify-between gap-4">
                <dt class="font-normal mb-1 sm:mb-0 text-gray-400">Delivery Method</dt>
                <dd class="font-medium text-white sm:text-end"><%= orderData.deliveryMethod %></dd>
          </dl>
            <dl class="sm:flex items-center justify-between gap-4">
                <dt class="font-normal mb-1 sm:mb-0 text-gray-400">Name</dt>
                <dd class="font-medium text-white sm:text-end"><%= orderData.name %></dd>
            </dl>
            <dl class="sm:flex items-center justify-between gap-4">
                <dt class="font-normal mb-1 sm:mb-0 text-gray-400">Address</dt>
                <dd class="font-medium text-white sm:text-end"><%= orderData.city %>, <%= orderData.country %></dd>
            </dl>
            <dl class="sm:flex items-center justify-between gap-4">
                <dt class="font-normal mb-1 sm:mb-0 text-gray-400">Phone</dt>
                <dd class="font-medium text-white sm:text-end"><%= orderData.phone %></dd>
            </dl>
        </div>
        <div class="flex items-center space-x-4">
            <a href="" class="text-white bg-blue-700  focus:ring-4 font-medium rounded-lg text-sm px-5 py-2.5 bg-primary-600 hover:bg-primary-700 focus:outline-none focus:ring-primary-800">Track your order</a>
            <a href="/" class="py-2.5 px-5 text-sm font-medium focus:outline-none rounded-lg border focus:z-10 focus:ring-4 focus:ring-gray-700 bg-gray-800 text-gray-400 border-gray-600 hover:text-white hover:bg-gray-700">Return to homepage</a>
        </div>
    </div>
  </section>