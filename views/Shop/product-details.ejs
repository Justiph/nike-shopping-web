
  
  <div class="container mx-auto p-4 bg-white">

    <div class="max-w-3xl mx-auto p-5">
      <h1 class="text-3xl text-black font-bold mb-4"><%= product.name %></h1>
      <img 
          src="<%= product.img %>"
          alt={product.name} 
          class="w-full h-auto rounded-lg mb-4 shadow-lg" 
      />
      <p class="text-lg text-gray-700 mb-2"><%= product.desc %></p>
      <p class="text-xl font-bold text-gray-800 mb-2">Price: $<%= product.price %> USD</p>
      <p class="text-md text-gray-600">Gender: <span class="font-semibold"><%= product.gender %></span></p>
      <div class="mt-4">
          <button class="bg-orange-500 text-white font-bold py-2 px-4 rounded-lg hover:bg-orange-600 transition duration-200">
              Add to Cart
          </button>
      </div>
    </div>

  </div>

